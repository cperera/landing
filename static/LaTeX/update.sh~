#! /bin/bash
for filename in *; do
	if [[ $filename =~ cv_([0-9-]*).* ]]
	then
		echo ${BASH_REMATCH[*]}
		match=${BASH_REMATCH[1]}
		newdate=`date +%Y-%m-%d`
		newname=${filename//$match/$newdate}
		mv $filename $newname
	fi
done	
